[
  {
    "id": "consolidate-repos",
    "subject": "Merge dadapter into acpreact — single package owns all code",
    "status": "pending",
    "description": "Move all dadapter functionality (bot.js, handler.js adapter pattern) into acpreact/adapters.js. dadapter becomes a thin entry-point that imports from acpreact. Delete redundant dead code (handler.js unused export, orphaned bot.js).",
    "blocking": [
      "expand-agents",
      "build-adapters",
      "build-gui",
      "update-dadapter"
    ],
    "blockedBy": [],
    "effort": "small",
    "category": "refactor",
    "acceptance": [
      "acpreact/adapters.js exists and exports createAdapter(type, config)",
      "dadapter/index.js imports from acpreact",
      "No duplicate code between repos"
    ],
    "edge_cases": [
      "package.json version compat",
      "ESM import paths"
    ]
  },
  {
    "id": "expand-agents",
    "subject": "Support all major CLI agents in services.js",
    "status": "pending",
    "description": "Add BUILTIN_ARG_BUILDERS for: kilo, opencode, gemini, aider, claude (claude-code CLI), codex, goose, amp, cursor. Each builder must produce correct non-interactive args. Rate-limit patterns for each. Parser must handle each agent's output format.",
    "blocking": [
      "build-gui"
    ],
    "blockedBy": [
      "consolidate-repos"
    ],
    "effort": "medium",
    "category": "feature",
    "acceptance": [
      "BUILTIN_ARG_BUILDERS contains: kilo, opencode, gemini, aider, claude, codex, goose, amp",
      "RATE_LIMIT_PATTERNS has entries for all agents",
      "parser.js handles both JSON-line and plain-text output"
    ],
    "edge_cases": [
      "agent not installed → ENOENT triggers fallback",
      "each agent uses different output format"
    ]
  },
  {
    "id": "build-adapters",
    "subject": "Build lightweight multi-platform chat adapters in acpreact/adapters.js",
    "status": "pending",
    "description": "Single adapters.js exporting createAdapter(type, config). Supported: discord (discord.js), telegram (pure fetch long-poll), slack (fetch + HTTP Events API), webhook (generic HTTP POST server). Each adapter: {start(), stop(), send(channelId, text), onMessage(fn)}. Gmail via nodemailer SMTP send-only.",
    "blocking": [
      "build-gui",
      "update-dadapter"
    ],
    "blockedBy": [
      "consolidate-repos"
    ],
    "effort": "large",
    "category": "feature",
    "acceptance": [
      "createAdapter('discord', {token}) works",
      "createAdapter('telegram', {token}) works via long-polling",
      "createAdapter('slack', {token, signingSecret}) works",
      "createAdapter('webhook', {port}) works",
      "createAdapter('gmail', {user, pass}) sends via SMTP",
      "All adapters implement onMessage(fn) + send(id, text) + start/stop"
    ],
    "edge_cases": [
      "missing token → clear error",
      "telegram poll timeout retry",
      "slack signature verification"
    ]
  },
  {
    "id": "build-gui",
    "subject": "Build TUI dashboard with ink (React for CLIs)",
    "status": "pending",
    "description": "acpreact/gui.jsx using ink + ink-select-input + ink-text-input. Shows: agent status (active/rate-limited), adapter connections, live message log, interactive prompt input. Accessible via bin.js --gui flag. Zerohop not available on npm; ink is the canonical lightweight alternative.",
    "blocking": [
      "update-dadapter"
    ],
    "blockedBy": [
      "expand-agents",
      "build-adapters"
    ],
    "effort": "large",
    "category": "feature",
    "acceptance": [
      "bin.js --gui launches ink TUI",
      "TUI shows connected adapters, active agent, live message log",
      "User can type prompt and see response inline",
      "TUI exits cleanly on q/Ctrl-C"
    ],
    "edge_cases": [
      "non-TTY env → skip GUI, use plain output",
      "large output truncation"
    ]
  },
  {
    "id": "update-dadapter",
    "subject": "Slim dadapter to thin entry-point importing from acpreact",
    "status": "pending",
    "description": "dadapter/index.js: import createAdapter + ACPProtocol from acpreact, wire Discord adapter to ACP process loop. Remove bot.js, handler.js. Update package.json. Push both repos to claude/refactor-adapter-gui-JAw1a.",
    "blocking": [],
    "blockedBy": [
      "build-adapters",
      "build-gui",
      "consolidate-repos"
    ],
    "effort": "small",
    "category": "refactor",
    "acceptance": [
      "dadapter/index.js < 50 lines",
      "bot.js and handler.js deleted",
      "dadapter package.json deps minimized",
      "Both repos pushed to feature branch"
    ],
    "edge_cases": [
      "dadapter still runnable standalone with DISCORD_BOT_TOKEN env var"
    ]
  }
]